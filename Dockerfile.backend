# 后端使用 Python 官方镜像
FROM python:3.12-slim

# 设置工作目录
WORKDIR /app

# 拷贝 Flask 后端代码
COPY backend/ /app

# 安装依赖
RUN pip install --no-cache-dir -r requirements.txt

# 启动 Flask 服务
CMD ["python", "main.py"]
